// import {instructions as konstructorFunctionList} from './app-konstructor'

/*
describe('konstructorFunctionList', () => {
	it('contains only functions', () => {
		for (const func of konstructorFunctionList) {
			expect(typeof func).toEqual('function')
		}
	})
})
*/
/*

describe('App konstructor', () => {

	it('initializes the App', () => {
		console.log(foo.mock.calls.length)
		// console.log(foo(),bar(),)
		// expect(fooFn).toBeCalled()
		// console.log(Kreator)
		// spy = Kreator.initialize_the_App
		// require('./app-konstructor')
		// konstruct_the_App_according_to()
		//expect(Kreator).toBeCalled()
		// expect(konstruct_the_App_according_to().Kreator.initialize_the_App()).toBe('initialize_the_App')
		// expect(Kreator.initialize_the_App()).toHaveBeenCalled()
	})

	it('adds tf Features to the App:', () => {

		test('Anyone can navigate the App', () => {

			expect(Middleware).toHaveProperty('add_tf_Features_to_the_App')
		})
	})
})
*/
